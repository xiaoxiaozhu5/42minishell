#include "ft_minishell.h"

void ft_process(t_env *env)
{
	(void)env;
	printf("Функция работающая после парсера, принимает структуру в которой будут данные и список (какие команды, редиректы, пайпы)\n");
}